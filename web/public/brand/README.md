# Brand assets (Radar Vacinal)

- `logo-ipads.png` — logo oficial IPADS (Downloads do usuário)
- `logo-jambro.png` — wordmark renderizado (Arial) nas cores do painel (`#252f49` / `#4a6aaf`). Substituir pelo arquivo oficial quando disponível.
- `logo-jambro.svg` — fallback vetorial do mesmo wordmark.
- `nascerimune-style-ref.css` — referência pública de tema (sem logo NascerImune)

Não incluir assets NascerImune neste painel.
